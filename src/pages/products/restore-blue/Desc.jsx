import React from "react";

export default () => {
  return <div>There's a grid of gems. Every gem has its color which can be changed in round-robin manner. Colors are changed in a group depending on a current mode.</div>;
};
