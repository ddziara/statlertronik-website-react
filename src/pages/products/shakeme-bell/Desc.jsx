import React from "react";

export default () => {
  return (
    <div>
      ShakeMe Bell is a bell simulator. A device is required to have
      accelerometer for this application to function. When a user moves the
      device then the bell and the clapper moves also and when they clash a
      sound can be heard. User can configure the bell and the floor appearance
      and also select sound.
    </div>
  );
};
